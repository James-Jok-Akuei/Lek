# Screenshots

Add your captured dashboard screenshots here for the submission/README:

- `login.png` — the admin login screen
- `overview.png` — the Overview dashboard (forecast banner + stat tiles)
- `predictions.png` — the Predictions page
- `users.png` — the Users page
- `alerts.png` — the Alerts log

**How to capture (macOS):** press `Cmd + Shift + 4`, then drag over the browser
area (or press Space to capture the whole window). Files save to your Desktop —
move them here and rename as above.
